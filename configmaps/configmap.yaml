apiVersion: v1
kind: ConfigMap
metadata:
  name: indykube-config
data:
  config.json: |
    {
      "environment" : "dev"
    }
# kubectl create configmap indykube-config --from-file ./golang/configs/config.json
